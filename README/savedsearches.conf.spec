# Webook alert action settings

action.custom_webhook = [0|1]
* Enable webhook action

action.custom_webhook.param.url = <string>
* URL to send the HTTP POST request to. Must be accessible from the Splunk server.

action.custom_webhook.param.raw_data = [1|0]
action.custom_webhook.param.notify = [1|0]
